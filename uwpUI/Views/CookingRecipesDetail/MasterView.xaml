<UserControl
    x:Class="uwpUI.Views.CookingRecipesDetail.MasterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:uwpUI.Helpers"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    IsFocusEngaged="False"
    IsFocusEngagementEnabled="False">

    <UserControl.Resources>
        <helpers:ItemGradeEnumToColorConverter x:Key="ItemGradeEnumToColorConverter"/>
    </UserControl.Resources>
    
    <Grid Height="45">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Source="{x:Bind ViewModel.Item.Img}"  VerticalAlignment="Center" />
        <StackPanel Grid.Column="1" Margin="{StaticResource SmallLeftMargin}" VerticalAlignment="Center">
            <TextBlock Text="{x:Bind ViewModel.Item.Name}" Style="{ThemeResource ListTitleStyle}"
                       Foreground="{x:Bind ViewModel.Item.Grade, Converter={StaticResource ItemGradeEnumToColorConverter}}"/>
            <TextBlock Text="{x:Bind ViewModel.Item.Id}" Style="{StaticResource ListSubTitleStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
